#!/usr/bin/env ansible-playbook -i hosts
- hosts: master
  vars:
    - vuls_path: /go/app
  roles:
    - Yasushikobayashi.ansible-direnv
  tasks:
    - include: 'tasks/main.yml'
    - name: set config
      copy:
        src: src/config.toml
        dest: '{{ vuls_path }}/config.toml'
